<section id="workflow" class="band" aria-labelledby="workflow-title">
  <div class="shell split-heading reveal-item">
    <header class="section-heading">
      <div><span class="section-index">01</span><span class="eyebrow">Operational workflow</span></div>
      <h2 id="workflow-title">Compress the noise without losing the evidence.</h2>
    </header>
    <p class="section-intro">Pamawas separates mechanical grouping from probabilistic reasoning. Each stage narrows the problem while retaining the source data an engineer needs to verify it.</p>
  </div>
  <div class="shell workflow-grid">
    <article class="workflow-step reveal-item" style="--reveal-delay: 0ms"><div class="step-top"><span>01</span><strong>2,300+</strong></div><h3>Collect raw signals</h3><p>Normalize alerts, logs, metrics, and deployment events into a shared evidence model.</p><span class="step-type deterministic">Deterministic</span></article>
    <article class="workflow-step reveal-item" style="--reveal-delay: 100ms"><div class="step-top"><span>02</span><strong>~17</strong></div><h3>Correlate incidents</h3><p>Group events by time window, service topology, and label overlap before using an LLM.</p><span class="step-type deterministic">Deterministic</span></article>
    <article class="workflow-step reveal-item" style="--reveal-delay: 200ms"><div class="step-top"><span>03</span><strong>1 brief</strong></div><h3>Investigate with bounds</h3><p>Query approved read-only tools, classify findings, and state what remains unknown.</p><span class="step-type bounded">Bounded AI</span></article>
  </div>
</section>
